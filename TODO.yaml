# Pending tasks for the port loop. `scripts/port-loop.py` pops one entry at a
# time and dispatches an agent to handle it before moving on to porting new
# upstream tests. Add entries at any position; remove an entry when it's done.
#
# Schema per entry:
#   - title: one-line summary
#     details: |
#       Multi-line body with context, pointers, acceptance criteria.

- title: Port conjecture/test_engine.py::test_handles_nesting_of_discard_correctly
  details: |
    Port `test_handles_nesting_of_discard_correctly` from
    `conjecture/test_engine.py`.  Upstream uses
    `monkeypatch.setattr(Shrinker, "shrink", Shrinker.remove_discarded)`
    to constrain the shrink loop to a single named pass — without that
    constraint the native full-shrinker finds the lex-smaller
    `(True, False)` minimum rather than the upstream-asserted
    `(True, True)`, because the discarded-span boundary information
    isn't tracked along the choice sequence.

    Port path: extend `Span` with a `discarded: bool` field and
    propagate it through `NativeConjectureData::stop_span_with_discard`
    into the shrinker's view of the test case (the existing
    `NativeTestCase.has_discards` flag is enough at the data level;
    the shrinker callback needs to see per-span discarded state too).
    Add a `remove_discarded` shrink pass to `src/native/shrinker/mod.rs`
    that mirrors `internal/conjecture/shrinker.py::remove_discarded`:
    collect non-overlapping discarded spans, delete their choice
    ranges in reverse, and `consider` the result.  Then either expose
    a way to run only that pass (e.g. via `NativeShrinker::fixate_shrink_passes`,
    coming with the new_shrinker cluster) or seed via
    `cached_test_function` plus an additional explicit
    `runner.shrinker_remove_discarded()` driver — pick whichever
    matches the new_shrinker port best so the two land coherently.

    Acceptance criteria:
      - `test_handles_nesting_of_discard_correctly` compiles and
        passes under `cargo test --features native --test hypothesis
        conjecture_engine::`.
      - The matching bullet in `SKIPPED.md` is updated to drop this
        test from the still-skipped list.

- title: Port conjecture/test_engine.py::test_terminates_shrinks
  details: |
    Port `test_terminates_shrinks` (parametrised on `n=1` and `n=5`)
    from `conjecture/test_engine.py`.  Upstream uses
    `monkeypatch.setattr(engine_module, "MAX_SHRINKS", n)` to cap the
    shrink loop and a long-running `HardToShrink` strategy (from
    `tests/common/strategies.py`) so the runner racks up exactly `n`
    successful shrinks before bailing with `ExitReason.max_shrinks`,
    persisting each pre-shrink intermediate as a `secondary_key` DB
    entry along the way.

    Port path:
      1. Track `runner.shrinks` — increment whenever
         `shrink_interesting_examples`'s callback observes a
         strictly-smaller interesting result for the current target,
         mirroring `engine.py` lines 698-704.
      2. Hook `MAX_SHRINKS` enforcement: when `shrinks >= max_shrinks`,
         raise an internal `RunIsComplete` sentinel that
         `shrink_interesting_examples` catches, sets
         `exit_reason = ExitReason::MaxShrinks`, and returns from the
         shrink loop.
      3. On each successful shrink: `save_choices(new)` to the
         primary DB key and `db.move_value(primary, secondary, old)` to
         demote the previous best to the secondary corpus, mirroring
         `engine.py::downgrade_choices`.
      4. Port `tests/common/strategies.py::HardToShrink` as a small
         Rust strategy (probably under `tests/hypothesis/common/`)
         that draws 100 bytes and only accepts a sequence one byte
         lower than the previously-accepted one — this gives the
         shrinker a long monotone descent so the `n=1` / `n=5`
         budgets land exactly.

    Acceptance criteria:
      - `test_terminates_shrinks` (both parameter rows, e.g.
        `_1` and `_5`) compiles and passes under
        `cargo test --features native --test hypothesis
        conjecture_engine::`.
      - The matching bullet in `SKIPPED.md` is updated to drop this
        test from the still-skipped list.

- title: Port conjecture/test_engine.py FailedHealthCheck cluster
  details: |
    Port the following tests from `conjecture/test_engine.py`,
    native-gated. Each wraps a `ConjectureRunner.run()` in
    `pytest.raises(FailedHealthCheck)` and asserts
    `str(HealthCheck.xxx) in str(e.value)`:
      - `test_fails_health_check_for_all_invalid`
      - `test_fails_health_check_for_large_base`
      - `test_fails_health_check_for_large_non_base`
      - `test_fails_health_check_for_slow_draws`
      - `test_health_check_too_slow_with_invalid_examples`
      - `test_health_check_too_slow_with_overrun_examples`
      - `test_too_slow_report`

    Port path: use the
    `hegel::__native_test_internals::fails_health_check(label, build)`
    free function (stub in `src/native/conjecture_runner.rs`) — fill
    it in to `catch_unwind` the build+run and assert the panic
    payload's string matches a per-label panic prefix. The native
    engine's `panic!("FailedHealthCheck: TooSlow …")` / `"…
    FilterTooMuch …"` strings already exist in
    `src/native/runner.rs`; extend them to cover
    `LargeBaseExample` / `DataTooLarge` as the tests require.

    Acceptance criteria:
      - All 7 tests compile and pass under
        `cargo test --features native --test hypothesis conjecture_engine::`.
      - The `::test_fails_health_check_for_all_invalid` through
        `::test_too_slow_report` bullet in `SKIPPED.md` is removed.

- title: Port conjecture/test_engine.py database-introspection cluster
  details: |
    Port the following tests from `conjecture/test_engine.py`,
    native-gated:
      - `test_clears_out_its_database_on_shrinking`
      - `test_database_clears_secondary_key`
      - `test_database_uses_values_from_secondary_key`

    Each uses `InMemoryExampleDatabase` with `.save(key, choices)` /
    `.fetch(key)` introspection plus `choices_to_bytes` /
    `choices_from_bytes`. The native analogs are
    `hegel::__native_test_internals::{InMemoryNativeDatabase,
    choices_to_bytes, choices_from_bytes}` — all are already exported.
    Fill in `NativeConjectureRunner::{save_choices, secondary_key,
    clear_secondary_key, reuse_existing_examples}` as needed.

    Acceptance criteria:
      - All 3 tests compile and pass.
      - The `::test_clears_out_its_database_on_shrinking` through
        `::test_database_uses_values_from_secondary_key` bullet in
        `SKIPPED.md` is removed.

- title: Port conjecture/test_engine.py new_shrinker cluster
  details: |
    Port the following tests from `conjecture/test_engine.py`,
    native-gated. Each seeds an initial buffer via
    `runner.cached_test_function(start)` then builds
    `runner.new_shrinker(last_data, lambda d: d.status == INTERESTING)`
    and drives a single named shrink pass via
    `fixate_shrink_passes([pass_name])`:
      - `test_can_remove_discarded_data`
      - `test_discarding_iterates_to_fixed_point`
      - `test_discarding_is_not_fooled_by_empty_discards`
      - `test_discarding_can_fail`
      - `test_can_write_bytes_towards_the_end`
      - `test_uniqueness_is_preserved_when_writing_at_beginning`
      - `test_dependent_block_pairs_can_lower_to_zero`
      - `test_handle_size_too_large_during_dependent_lowering`
      - `test_block_may_grow_during_lexical_shrinking`
      - `test_lower_common_node_offset_does_nothing_when_changed_blocks_are_zero`
      - `test_lower_common_node_offset_ignores_zeros`
      - `test_cached_test_function_returns_right_value`
      - `test_cached_test_function_does_not_reinvoke_on_prefix`
      - `test_branch_ending_in_write`
      - `test_exhaust_space`
      - `test_discards_kill_branches`
      - `test_number_of_examples_in_integer_range_is_bounded`
      - `test_does_not_cache_extended_prefix`
      - `test_does_cache_if_extend_is_not_used`
      - `test_does_result_for_reuse`
      - `test_does_not_use_cached_overrun_if_extending`
      - `test_uses_cached_overrun_if_not_extending`
      - `test_can_be_set_to_ignore_limits`
      - `test_overruns_with_extend_are_not_cached`

    Port path: fill in `NativeConjectureRunner::{cached_test_function,
    new_shrinker, generate_novel_prefix, tree}`, plus
    `NativeShrinker::{shrink, fixate_shrink_passes, current_nodes}`.
    Named pass strings (e.g. `"minimize_individual_blocks"`,
    `"lower_common_node_offset"`) map to entries in
    `src/native/shrinker/mod.rs`'s pass table — thread the single-pass
    entry point through rather than duplicating the full shrink loop.

    Like the runner-bookkeeping cluster this may land as several
    narrower commits; split if the set feels too large.

    Acceptance criteria:
      - All 24 tests compile and pass under
        `cargo test --features native --test hypothesis conjecture_engine::`.
      - The `::test_can_remove_discarded_data` through
        `::test_overruns_with_extend_are_not_cached` bullet in
        `SKIPPED.md` is removed.

- title: Port conjecture/test_engine.py pareto-front cluster
  details: |
    Port the following tests from `conjecture/test_engine.py`,
    native-gated. Each asserts on `runner.pareto_front` /
    `dominance` / `runner.optimise_calls_count`:
      - `test_populates_the_pareto_front`
      - `test_pareto_front_contains_smallest_valid`
      - `test_replaces_all_dominated`
      - `test_does_not_duplicate_elements`
      - `test_includes_right_hand_side_targets_in_dominance`
      - `test_smaller_interesting_dominates_larger_valid`
      - `test_runs_optimisation_even_if_not_generating`
      - `test_runs_optimisation_once_when_generating`
      - `test_does_not_run_optimisation_when_max_examples_is_small`

    Port path: fill in `NativeConjectureRunner::pareto_front` and the
    `dominance` free function. Pareto bookkeeping lives in Hypothesis's
    `internal/conjecture/pareto.py` — mirror the `ParetoFront` /
    `DominanceRelation` structure there. `src/native/optimiser.rs`
    already covers the hill-climb side; extend it (or add
    `src/native/pareto.rs`) for the front-tracking side.

    Acceptance criteria:
      - All 9 tests compile and pass.
      - The `::test_populates_the_pareto_front` through
        `::test_does_not_run_optimisation_when_max_examples_is_small`
        bullet in `SKIPPED.md` is removed.

- title: Port conjecture/test_engine.py::test_debug_data
  details: |
    Port `test_debug_data` from
    `resources/hypothesis/hypothesis-python/tests/conjecture/test_engine.py`.
    Upstream uses `capsys` to capture stdout and assert on
    `Verbosity.debug` diagnostic output from a
    `ConjectureRunner.run()` call.

    Port path: use `TempRustProject` (see `tests/test_output.rs` and
    `tests/test_draw_named.rs`) to run a test with
    `Verbosity::Debug` in a subprocess and capture stderr, then
    assert on the expected diagnostic lines. The
    `NativeConjectureRunner` wrapper doesn't need additional stubs
    for this — the existing `native_run` debug output already covers
    the content. This test may end up native-gated or not depending
    on whether the `TempRustProject` path works on both backends.

    Acceptance criteria:
      - `test_debug_data` compiles and passes.
      - The `::test_debug_data` bullet in `SKIPPED.md` is removed.

- title: Port conjecture/test_engine.py::test_mildly_complicated_strategies LargeFlag row
  details: |
    The `st.sampled_from(enum.Flag("LargeFlag", ...))` row of
    `test_mildly_complicated_strategies` in
    `resources/hypothesis/hypothesis-python/tests/conjecture/test_engine.py`
    is currently skipped because Python's `enum.Flag` has no direct
    Rust analog. Build a small local bitflags-style type (e.g. via
    the `bitflags!` crate already in use, or a plain `#[derive(Clone,
    Copy)] struct LargeFlag(u64)`) with 64 distinct flag values, wrap
    it in `gs::sampled_from(all_flag_combinations)`, and port the
    `minimal(gen, |f| bit_count(f.value) > 1)` shape. No new native
    wrapper functionality needed.

    Acceptance criteria:
      - The port compiles and passes under
        `cargo test --test hypothesis conjecture_engine::`.
      - The `::test_mildly_complicated_strategies` (LargeFlag row)
        bullet in `SKIPPED.md` is removed.
