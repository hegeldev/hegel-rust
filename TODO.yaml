# Pending tasks for the port loop. `scripts/port-loop.py` pops one entry at a
# time and dispatches an agent to handle it before moving on to porting new
# upstream tests. Add entries at any position; remove an entry when it's done.
#
# Schema per entry:
#   - title: one-line summary
#     details: |
#       Multi-line body with context, pointers, acceptance criteria.

  - title: "[CI] check-release: RELEASE.md already exists on main"
    details: |
      The `Check release` job in CI failed (run 24749868873) on PR 188.
      The `.github/scripts/release.py check` script raised a ValueError
      because RELEASE.md already exists on `main`.

      Verbatim log:
        Traceback (most recent call last):
          File "/home/runner/work/hegel-rust/hegel-rust/.github/scripts/release.py", line 256, in <module>
            check(args.base_ref)
          File "/home/runner/work/hegel-rust/hegel-rust/.github/scripts/release.py", line 103, in check
            raise ValueError(
        ValueError: RELEASE.md already exists on main. It's possible the CI job responsible for cutting a new release is in progress, or has failed.
        ##[error]Process completed with exit code 1.

      Failing command: `python .github/scripts/release.py check "$BASE_REF"`
      (env BASE_REF=main), in workflow job "check release / Check release".

      Note: this same failure has already been triaged and removed twice
      from TODO.yaml in recent commits (de8b2a4, fca809d) with the
      message "Remove TODO for check-release failure (human intervention
      required)". The fix agent should likely just remove this entry
      again — the underlying issue requires a human to either cut the
      pending release or remove the stale RELEASE.md from main, neither
      of which is appropriate for an automated agent.
