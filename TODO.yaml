# Pending tasks for the port loop. `scripts/port-loop.py` pops one entry at a
# time and dispatches an agent to handle it before moving on to porting new
# upstream tests. Add entries at any position; remove an entry when it's done.
#
# Schema per entry:
#   - title: one-line summary
#     details: |
#       Multi-line body with context, pointers, acceptance criteria.

- title: Switch native StringChoice to a codepoint-indexed text representation
  details: |
    `ChoiceValue::String` in `src/native/core/choices.rs:397` currently stores
    text values as a Rust `String` (UTF-8 bytes). Every engine operation on
    it — `StringChoice::validate` (:367), `::sort_key` (:379), the shrinker
    in `src/native/shrinker/strings.rs`, and the state machinery in
    `src/native/core/state.rs` — has to re-decode UTF-8 into codepoints
    before doing anything useful, because the engine's model of a string is
    a sequence of Unicode codepoints (shortlex-ordered by
    `codepoint_key(cp)`), not a sequence of bytes.

    The desired representation is `Vec<u32>` (a vector of raw Unicode
    codepoints in `0..=0x10FFFF`), so that len/index/sort_key are
    O(1)/O(n) in the codepoint count rather than O(n) UTF-8 decodes, and
    so that the type signature honestly reflects what the engine is
    doing. `Vec<char>` is NOT the right type: Rust's `char` is a Unicode
    scalar value and disallows surrogates (`U+D800..=U+DFFF`), but the
    engine needs to be able to reason internally about any codepoint
    including surrogates — the no-surrogate filter belongs at the
    boundary where the engine hands a `String` back to the user, not in
    the core representation.

    Scope:
    - Change `ChoiceValue::String(String)` to `ChoiceValue::String(Vec<u32>)`
      (name TBD — `Text`/`Codepoints` also reasonable).
    - Update all construction/read sites under `src/native/` to work in
      `&[u32]` / `Vec<u32>` directly rather than round-tripping through
      `String`.
    - Public API boundaries (the `TextGenerator`-facing parts) still hand
      the user a `String`; the `u32 -> char -> String` conversion happens
      at that boundary, with the surrogate filter applied there (not in
      the engine core).
    - Update embedded tests under `tests/embedded/native/` that currently
      assert on `String` values.
    - Leave the SERVER path (`src/generators/strings.rs` schema plumbing)
      untouched; this is a native-engine representation change only.

    Acceptance:
    - `cargo test` passes.
    - `HEGEL_SERVER_COMMAND=/bin/false cargo test --features native` passes.
    - No `String` field remains on `StringChoice`-associated types in the
      native engine's internal data model.

- title: Adopt num-bigint for arbitrary-precision integers in the native engine
  details: |
    Both pbtkit and Hypothesis lean on Python's bignum int type in
    several places the native engine has to match — most prominently
    the shortlex index code (`to_index`/`from_index`/`codepoint_rank`
    in `resources/pbtkit/src/pbtkit/core.py`, `text.py`, `bytes.py`),
    where the index for e.g. `BytesChoice{max_size=16}` is bounded
    above by 256^16 ≈ 2^128, and realistic bounds (larger `max_size`,
    wider codepoint ranges) blow past `u128` immediately. Shrinking
    passes that manipulate those indices (e.g. pbtkit
    `shrinking/index_passes.py`) have the same requirement.

    Currently `src/native/` avoids these by just not implementing the
    index API, but the tightened skip policy — and the `tests/pbtkit/
    text.rs` revisit TODO above — routes those tests back into the
    native engine, where they'll need real arbitrary-precision
    arithmetic to pass.

    Plan:
    - Add `num-bigint` (and likely `num-traits`, `num-integer`) to
      `Cargo.toml` under the `native` feature. Re-export what
      `src/native/` uses through a small internal module so later
      swaps (e.g. to `malachite`) are localised.
    - Replace hand-rolled big-integer stand-ins under `src/native/`
      with `BigUint`/`BigInt`.
    - Use it as the signature for `to_index`/`from_index` when they
      land (see the text.rs revisit TODO).
    - Decide deliberately whether bignum leaks into the public native
      API or stays `pub(crate)` — default to `pub(crate)`, only
      promote if a public test genuinely needs it.

    Acceptance:
    - Native-mode tests that require arbitrary-precision arithmetic
      compile and pass without `u128`-width clamping.
    - Server mode is untouched (the server path has no bignum
      requirement).
    - `src/native/` has no ad-hoc big-integer helpers — all
      arithmetic routes through `num`.

- title: Port Python's unicodedata (from UnicodeData.txt) for category queries
  details: |
    Hypothesis's text generation leans on Python's `unicodedata`
    module — in particular `unicodedata.category(c)` for General
    Category filtering (e.g. `characters(whitelist_categories=...)`),
    plus a few other queries (`name`, `decomposition`) used in
    corners. The current native-side approximation in
    `src/native/schema/text.rs` (`char_in_category` around :374) is
    a hand-rolled match on Rust's built-in `char::is_*` methods and
    is demonstrably wrong for most categories. Examples of current
    mis-matches:

    - `Po` (Other_Punctuation) is checked with
      `c.is_ascii_punctuation()`, so it misses every non-ASCII
      punctuation codepoint and also accepts non-`Po` ASCII
      punctuation like `_` (which is actually `Pc`) and `-`
      (actually `Pd`).
    - `Sm` (Math_Symbol) is hardcoded to `+ < = > | ~`, missing the
      entire Unicode math range.
    - `Pi`/`Pf` (Initial/Final_Punctuation), `Lt` (Titlecase_Letter),
      `Lm`/`Lo` (Modifier/Other_Letter) are all approximated with
      generic `is_alphabetic`/`is_ascii_punctuation` checks that
      don't align with the actual categories.
    - `Cs` returns false, which happens to be right for `char` but
      becomes wrong under the codepoint-vec TODO above where
      surrogates are representable.

    Plan: port enough of Python's `unicodedata` module to support the
    category queries Hypothesis actually uses, based directly on
    the authoritative data file:

      https://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt

    - Vendor a snapshot of `UnicodeData.txt` under `src/native/`
      (or fetch it at `build.rs` time — decide based on
      reproducibility preferences). Pin the Unicode version in a
      comment.
    - Write a build-time or one-shot generator that compiles the
      file into a compact lookup table keyed by codepoint, returning
      the two-character General Category and whatever other fields
      are needed.
    - Expose a module (e.g. `src/native/unicodedata.rs`) with a small
      API: `fn general_category(cp: u32) -> GeneralCategory` plus
      grouping helpers (`is_in_group("L")` → matches `Lu|Ll|Lt|Lm|
      Lo`, etc.) to match Hypothesis's prefix-matching semantics.
    - Replace `src/native/schema/text.rs::char_in_category` with
      this, and delete the approximations.
    - Audit the Hypothesis test corpus for any other `unicodedata`
      call that lands in native mode, and port each as needed.

    Acceptance:
    - `src/native/schema/text.rs::char_in_category` is gone (or is
      a thin wrapper over the new module).
    - Category queries produce the same answer as Python's
      `unicodedata.category` for every codepoint in `0..=0x10FFFF`
      (verify with a cross-process round-trip test, or by baking a
      sample into an embedded test).
    - Hypothesis tests that filter by General Category pass in
      native mode.
