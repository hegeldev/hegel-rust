# Pending tasks for the port loop. `scripts/port-loop.py` pops one entry at a
# time and dispatches an agent to handle it before moving on to porting new
# upstream tests. Add entries at any position; remove an entry when it's done.
#
# Schema per entry:
#   - title: one-line summary
#     details: |
#       Multi-line body with context, pointers, acceptance criteria.

- title: "[CI] check release — missing RELEASE.md"
  details: |
    The `check release` job fails because PR 188 has no RELEASE.md file.
    The release.py script requires every PR to include one.

    Log (run 24674157469, job "check release", step "Check release"):
      ValueError:
        * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
        *                                                                  *
        *   Every pull request to hegel-rust requires a RELEASE.md file.   *
        *  You can find an example and instructions in RELEASE-sample.md.  *
        *                                                                  *
        * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

    Fix: add a RELEASE.md following the format in RELEASE-sample.md.
    Use the `changelog` skill for style guidance.
