# Pending tasks for the port loop. `scripts/port-loop.py` pops one entry at a
# time and dispatches an agent to handle it before moving on to porting new
# upstream tests. Add entries at any position; remove an entry when it's done.
#
# Schema per entry:
#   - title: one-line summary
#     details: |
#       Multi-line body with context, pointers, acceptance criteria.

  - title: "[CI] coverage: small native files — mod.rs + combinators.rs + tree.rs"
    details: |
      Part of the coverage ratchet gate on PR #188 (`just check-coverage`).
      Original issue had 4510 uncovered lines across src/native/ and
      src/generators/combinators.rs; it has been split per-file. This entry
      covers the smallest three files combined:

        - src/generators/combinators.rs   (18 uncovered lines — `permutations`)
        - src/native/mod.rs                (2 uncovered lines — `with_native_tc`)
        - src/native/tree.rs               (37 uncovered lines)

      Approach: add tests under tests/embedded/native/ (mirroring src/) or
      tests/ integration tests. Consult `coverage` skill. Do not add // nocov
      without explicit human permission. Do not bump
      `.github/coverage-ratchet.json`.

      Acceptance: these files report 0 uncovered code lines in
      `just check-coverage` output.

  - title: "[CI] coverage: src/native/schema/special.rs"
    details: |
      Part of the PR #188 coverage ratchet split. 85 uncovered code lines.

      Acceptance: src/native/schema/special.rs reports 0 uncovered code
      lines in `just check-coverage`.

  - title: "[CI] coverage: src/native/shrinker/sequence.rs + shrinker/deletion.rs"
    details: |
      Part of the PR #188 coverage ratchet split.

        - src/native/shrinker/sequence.rs (88 uncovered code lines)
        - src/native/shrinker/deletion.rs (98 uncovered code lines)

      Acceptance: both files report 0 uncovered code lines in
      `just check-coverage`.

  - title: "[CI] coverage: src/native/shrinker/strings.rs"
    details: |
      Part of the PR #188 coverage ratchet split. 143 uncovered code lines.
      Largest remaining shrinker file; test via CachedTestFunction with
      string-bearing generators.

      Acceptance: src/native/shrinker/strings.rs reports 0 uncovered code
      lines in `just check-coverage`.

  - title: "[CI] coverage: src/native/schema/mod.rs"
    details: |
      Part of the PR #188 coverage ratchet split. 165 uncovered code lines.
      Schema entry point — dispatch table, encode/decode, shared helpers.

      Acceptance: src/native/schema/mod.rs reports 0 uncovered code lines
      in `just check-coverage`.

  - title: "[CI] coverage: src/native/shrinker/integers.rs"
    details: |
      Part of the PR #188 coverage ratchet split. 172 uncovered code lines.
      Integer-shrinking passes; exercise via CachedTestFunction.

      Acceptance: src/native/shrinker/integers.rs reports 0 uncovered code
      lines in `just check-coverage`.

  - title: "[CI] coverage: src/native/schema/text.rs"
    details: |
      Part of the PR #188 coverage ratchet split. 197 uncovered code lines.
      Text/string schema — interacts with unicodedata.

      Acceptance: src/native/schema/text.rs reports 0 uncovered code lines
      in `just check-coverage`.

  - title: "[CI] coverage: src/native/runner.rs"
    details: |
      Part of the PR #188 coverage ratchet split. 199 uncovered code lines.
      Top-level loop that drives generation + shrinking. Some logic is
      already tested via tests/embedded/native/runner_tests.rs and
      tests/test_native.rs — find the gaps first.

      Acceptance: src/native/runner.rs reports 0 uncovered code lines in
      `just check-coverage`.

  - title: "[CI] coverage: src/native/core/state.rs"
    details: |
      Part of the PR #188 coverage ratchet split. 306 uncovered code lines.
      Core `NativeTestCase` / data-drawing state machine; partial coverage
      already via tests/embedded/native/state_tests.rs.

      Acceptance: src/native/core/state.rs reports 0 uncovered code lines
      in `just check-coverage`.

  - title: "[CI] coverage: src/native/core/choices.rs"
    details: |
      Part of the PR #188 coverage ratchet split. 423 uncovered code lines.
      Core Choice encoding/decoding; tests/embedded/native/choices_tests.rs
      already exists (~800 lines) — extend to cover remaining branches.

      Acceptance: src/native/core/choices.rs reports 0 uncovered code lines
      in `just check-coverage`. If this entry proves too large for one
      invocation, sub-split by choice kind (integer/float/bytes/string).

  - title: "[CI] coverage: src/native/database.rs"
    details: |
      Part of the PR #188 coverage ratchet split. 518 uncovered code lines.
      tests/embedded/native/database_tests.rs already exists (~1250 lines)
      but large chunks remain uncovered — audit the reported lines and
      extend. If too large, sub-split by feature (save/load/gc/migration).

      Acceptance: src/native/database.rs reports 0 uncovered code lines in
      `just check-coverage`.

  - title: "[CI] coverage: src/native/schema/regex.rs"
    details: |
      Part of the PR #188 coverage ratchet split. 559 uncovered code lines.
      tests/embedded/native/schema/regex_tests.rs is only 49 lines — needs
      significant expansion. Cross-check against Hypothesis's
      `hypothesis.internal.conjecture.shrinking.regex` or equivalent.

      Acceptance: src/native/schema/regex.rs reports 0 uncovered code lines
      in `just check-coverage`. Consider sub-splitting if the file's size
      makes a single invocation impractical.

  - title: "[CI] coverage: src/native/re/parser.rs"
    details: |
      Part of the PR #188 coverage ratchet split. 892 uncovered code lines
      (the largest uncovered file). tests/embedded/native/re_parser_tests.rs
      is 644 lines and does cover some of the parser but big gaps remain.
      Sub-split by parser feature (character classes / quantifiers /
      anchors / groups / alternation / lookaround / named groups) if one
      invocation can't clear it.

      Acceptance: src/native/re/parser.rs reports 0 uncovered code lines
      in `just check-coverage`.

  - title: "[CI] coverage: final verification — just check-coverage passes on PR #188"
    details: |
      Umbrella entry for the PR #188 coverage ratchet split. After all
      per-file entries above are cleared, run `just check-coverage` once
      more and confirm it exits 0. Handle any stragglers (e.g. the
      nocov ratchet count may need to drop since we removed files, or a
      cross-file line may appear uncovered). Verify the `coverage` job on
      the next CI run for PR #188 is green.
