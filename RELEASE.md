RELEASE_TYPE: patch

Bump our pinned hegel-core to [0.2.2](https://github.com/hegeldev/hegel-core/releases/tag/v0.2.2), incorporating the following change:

> Add support for the `derandomize` and `database` settings to the `run_test` payload in the protocol.
>
> As a result, this release also bumps the protocol version to `0.7`.
>
> — [v0.2.2](https://github.com/hegeldev/hegel-core/releases/tag/v0.2.2)
