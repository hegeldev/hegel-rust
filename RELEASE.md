RELEASE_TYPE: patch

This patch adds an internal benchmark suite covering the native engine's end-to-end generation and shrinking paths. There is no user-facing change; the benchmarks exist to measure and guard the performance of the engine itself.
