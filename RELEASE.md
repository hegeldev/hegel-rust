RELEASE_TYPE: patch

This release fixes a minor problem in our docs where the docs.rs suggested installing an old version of hegel.

(And, more importantly, updates our build infra to prevent this problem from cropping up again in future)
