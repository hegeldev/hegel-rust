[package]
name = "paper-eval"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
hegel = { path = "../..", package = "hegeltest", features = ["__bench"] }
hegel-c = { path = "../../hegel-c", package = "hegeltest-c", features = ["__bench"] }
tempfile = "3.0"

[workspace]

[profile.release]
debug = false
